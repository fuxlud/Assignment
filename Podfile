# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SimilarWeb_Assignment' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
    pod 'Alamofire', '5.4.1' # Latest pods. Yet attaching them to a specific version for stability
    pod 'AlamofireImage', '4.1.0' 
  # Pods for SimilarWeb_Assignment

  target 'SimilarWeb_AssignmentTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
